<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QuakeInfo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app_bar</item>
        <item name="colorPrimaryVariant">@color/app_bar</item>
        <item name="colorOnPrimary">@color/app_bar</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/status_bar</item>
        <item name="colorSecondaryVariant">@color/status_bar</item>
        <item name="colorOnSecondary">@color/status_bar</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/status_bar</item>
        <!-- Customize your theme here. -->
    </style>
</resources>